name: ac_code_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 5.7.4-2024021802

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  ac_code_metrics: 5.7.4-2024021802

dev_dependencies:
  lints: ^1.0.1
